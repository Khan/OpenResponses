module.exports = {
  // I know this key ends up getting sent to clients anyway (I'd use a proxy server if I really wanted to avoid that), but I figure I'll at least keep it from being directly searchable on Github.
  FIREBASE_API_KEY: "<key here>",
};
